# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This project is intentionally pinned to SDK 54: the iOS App Store version of
Expo Go is stuck at SDK 54 (Apple approval delays, see Expo changelog May
2026), and SDK 54 is the newest version that runs in Expo Go on a real iPhone.
Do not upgrade the SDK without checking what Expo Go on the App Store supports.
